mutation CreateFile($data: FileCreateInput!) {
    createFile(data: $data) {
        id
        uploadPath
        storageKey
    }
}

query GetForms {
  settings {
    capture {
      requiredForms {
        forms {
          formType {
            displayName
          }
        }
      }
    }
  }
}
